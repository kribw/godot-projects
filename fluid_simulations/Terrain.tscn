[gd_scene load_steps=5 format=2]

[ext_resource path="res://models/terrain.glb" type="PackedScene" id=1]
[ext_resource path="res://terreng.gd" type="Script" id=2]

[sub_resource type="Shader" id=1]
code = "shader_type spatial;
//render_mode world_vertex_coords;

varying vec3 my_color;
uniform sampler2D sampler;
uniform float test;
uniform mat4 global_transform;

vec3 get_color(float position) {
	//return texture(sampler, vec2(position, ))
	return vec3(1.0);
}

void vertex() {
	vec4 world_vertex = vec4(VERTEX, 1.0);//CAMERA_MATRIX * vec4(VERTEX, 1.0);

	// We want to color x between 60 and 171
	// y between 42 and 158
	// z between 31 and 84
	
	vec4 pos = global_transform * vec4(VERTEX, 1.0);
	
	if(pos.x < 271.0 && pos.x > -200.0) {
		// blue color
		my_color = vec3(0.2, 0.5, 0.8);
	} else {
		// dark pink color
		my_color = vec3(0.6, 0.1, 0.2);		
	}
	
	//COLOR.rgb = my_color;
}

void fragment() {
	ALBEDO.rgb = my_color;
}"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )
shader_param/test = null
shader_param/global_transform = null

[node name="terrain" instance=ExtResource( 1 )]

[node name="terreng" parent="." index="0"]
material/0 = SubResource( 2 )
script = ExtResource( 2 )
